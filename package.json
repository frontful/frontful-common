{
  "name": "frontful-common",
  "version": "2.0.3",
  "private": true,
  "description": "Common configuration provider for Frontful infrastructure",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/frontful/frontful-common.git"
  },
  "scripts": {
    "build": "yarn test && babel-preset-frontful build",
    "deploy": "yarn test && babel-preset-frontful deploy",
    "test": "yarn test:eslint",
    "test:eslint": "node ./node_modules/eslint/bin/eslint.js --max-warnings 0 ."
  },
  "dependencies": {
    "frontful-config": "2.x",
    "object-path": "^0.11.4"
  },
  "devDependencies": {
    "babel-preset-frontful": "2.x",
    "eslint-config-frontful": "2.x"
  }
}
